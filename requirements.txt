Flask==3.0.0
opencv-python==4.8.0.76
numpy==1.24.4
deepface==0.0.79
sqlite3== # (Included in Python standard library, no version needed)
python-dateutil==2.8.2
gunicorn==21.2.0  # Required for production WSGI server on Render
Pillow==10.0.1  # For image processing
scikit-learn==1.3.2  # Required by DeepFace for some models
tensorflow==2.13.0  # Required by DeepFace (or tensorflow-cpu for lighter install)
mtcnn==0.1.1  # Optional (if using MTCNN detector)
retina-face==0.0.14  # Optional (if using RetinaFace detector)